# Poly Bot v1

A minimal scaffold for a Polymarket bot.

## Structure

- `main.py` - entrypoint
- `src/` - bot modules
- `.env.example` - sample environment variables

## Run

```bash
python3 main.py
```

If you want to use a local `.env` file, copy `.env.example` to `.env` and adjust the values.
